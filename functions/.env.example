# Endpoint of the myscheme.gov.in MCP connector (scheduled sync target).
# Leave empty to keep seeded scheme data untouched.
MCP_SCHEMES_URL=

# Groq API key for chat completion (server-side only, never expose to frontend)
GROQ_API_KEY=

# ElevenLabs API key for text-to-speech (server-side only, never expose to frontend)
ELEVENLABS_API_KEY=
